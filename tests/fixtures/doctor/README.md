# Relay doctor fixtures

Doctor fixtures use synthetic temporary paths and values only. They must not
contain home-directory prefixes, credentials, private keys, SQL dumps, or
absolute paths from a source checkout.
