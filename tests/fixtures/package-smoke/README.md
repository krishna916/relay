# Installed package smoke fixture

The package smoke harness creates its own prefix, unrelated working directory,
temporary database, and tarball. It never resolves the repository's local
executable or durable database.
